# BubbleSortArrayIncreasingOrder
# This Java program uses the Bubble Sort array to sort the elements in the array from lowest to highes
# Everything was coded instead of using the Array.sort() method in Java
